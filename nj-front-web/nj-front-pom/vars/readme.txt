vars文件里的变量定义用于对web工程中的静态文件（html,js,css）进行替换，我们一共准备了3套变量，
分别是开发环境、PRE、和生产环境，maven在打包前会对src/main/webapp下的静态资源文件中的变量进
行替换。

由于开发时用的是未替换的文件，所以，如果是主机名的话，请在hosts中加入变量名为主机名，如：
127.0.0.1		${marketServer}

如果你的项目没有需要替换的话，可以删除相关的变量文件和pom中相关章节